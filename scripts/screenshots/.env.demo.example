# Throwaway environment for the screenshot demo instance.
# Copy to .env.demo (gitignored) before running the demo stack.
# These credentials are disposable — the instance is destroyed after capture.

DB_ROOT_PASSWORD=demo_root_pw
DB_NAME=HamLogDB
DB_USER=hamlog
DB_PASSWORD=demo_pw
PORT=8060
JWT_SECRET=demo-screenshots-throwaway-secret-not-for-production
